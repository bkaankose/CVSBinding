<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CVSBinding.CVSPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:collections="using:CommunityToolkit.Mvvm.Collections"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CVSBinding"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource
            x:Name="CVS"
            IsSourceGrouped="True"
            Source="{x:Bind VM.MailItems, Mode=OneWay}" />
    </Page.Resources>

    <Grid Padding="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Click="AddClicked" Content="Add Item" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{x:Bind CVS.View, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:MyItem">
                    <TextBlock Text="{x:Bind Title}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="True" GroupPadding="10,10,10,0" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.GroupStyle>
                <!--
                    HidesIfEmpty="True" Makes it completely invisible.
                    Groups are added to CVS, but items are not added to those groups.
                -->
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock
                                FontSize="24"
                                FontWeight="Bold"
                                Text="Test header" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</Page>
